{
  "name": "@wcb/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "prisma generate && tsc -b",
    "generate": "prisma generate",
    "migrate:dev": "prisma migrate dev",
    "test": "tsx --test src/ingest.test.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.2.0",
    "@wcb/shared": "workspace:*"
  },
  "devDependencies": {
    "prisma": "^6.2.0",
    "tsx": "^4.19.2"
  }
}
